# Hoist all dependencies to root node_modules.
# Many packages (Apollo, vega, cytoscape, Observable Plot) have transitive deps
# that Vite needs to resolve from the project root during dev pre-bundling.
shamefully-hoist=true
